The program was written with version 3.11.2 of python.
The following pip modules are required and can be obtained by running the following commands:
pip install pandas
pip install numpy
pip install scipy 
pip install scikit-learn
pip install sklearn

The data path and other parameters can be specified in Main.py.
To run the simulation run the following command:
python Main.py